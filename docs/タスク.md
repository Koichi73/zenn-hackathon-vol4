# 残りのタスク

## マスト要件
### ダッシュボード周り
- [x] ログインしてない状態で画像アップロードさせない
    - そもそもダッシュボードに入れない？
    - 未ログインで動画アップロードするとエラー起きる
    - ログインしてない状態でダッシュボードに行くと自動でデフォルトユーザーになるロジックで解決
- [x] テストログインユーザー作っとく
- [x] テスト動画埋め込んでおく
- [x] `completed` が何を意味しているか分かりにくいから変える。理想は `unshare`, `share` とか


### 編集画面
#### Edit
- [x] Stepの削除機能の削除
- [x] マスクやハイライトを追加・削除した時のfirestoreとのロジック
    - ハイライトは現状1つしか作れないロジックなので、どうしようか
- [x] リロードしたら動画が表示されない
- [x] 「Click on a mask/frame to resize or move. Click empty space to deselect」を消す
- [x] 「Description」の文字もいらない気がする。見たらわかる
- [x] 動画はデフォルトで閉じてるように
- [x] 動画の×を消して、最小化を現在の×の使用に
- [x] 動画をホバーした時の矢印をなくす
- [x] フレームごとの画像解析を非同期に
- [x] タイトルの表示
- [x] レスポンシブ
- [ ] マスクとハイライトが一つもない時の「No masks ...」を削除する

#### Preview
- [x] 以下を消す
    - 「Generated Manual」
    - 「Video Manual」
    - Timestamp(?)
        - 閲覧者は動画を見ないのでタイムスタンプは不要と感じた
    - 「[!NOTE] This image contains 5 masked area(s) ...」

#### その他
- [x] shareボタンを押したらリンク表示で良さそう(GENERATE LINKは不要)

### 共有画面
- [x] previewと同じで不要なもの消す

### デプロイ
- [x] Cloud Runとかにデプロイ

### その他
- [ ] 追加のinput動画を用意する
    - 1つ作成済み(WordPress)
    - もう1つ作成する(Google製品で、短めのもの)
- [ ] zenn記事作成
- [ ] youtube動画作成(3分)
- [x] 429エラーへの対策
- [ ] README作成
- [ ] 不要なファイルの整理

## 時間があったら
- [x] ダッシュボードの一番最初に動画アップロードエリアを入れる
- [x] ダッシュボードの表示を早くする
    - 今は全てDLしてから表示
    - 許容できる速度なので解決
- [x] 動画解析のローディングに説明加える
    - ここでアプリのTipsを入れたい
    - 10秒間隔で切り替わると最高
    - 例：
        - Geminiは1秒ごとに動画解析をします。収録の際はゆっくり操作してください。
        - アカウント未作成でも、デフォルトユーザーとしてログインできます。
        - 動画解析中はブラウザを閉じても問題ありません。
        - ダッシュボードのタグは、マニュアルを共有したらshareに、停止したらprivateになります。
- [x] 目次をちょいアピール
    - 目次は削除になったため解決
- [x] shareボタン押したらリンク表示は一瞬でできないかな？何でロードしてる？→できる
    - 許容できるくらいになったので解決
- [x] すでにshare済みの場合にもう一回押したら、share解除できるようにする
    - ダッシュボードにshare中か否か入れる？
- [x] タイトルの文字をちょっと大きく
- [x] 編集画面のマスクとかを選択したら、ずっと選択状態になる。他の適当な箇所をクリックしたら選択解除にしたい
- [x] ログイン状態か非ログインか見分けがつかない
    - デフォルトログインによって非ログインがなくなったため解決
- [x] ダッシュボードのページネーション
- [x] 429エラーの通知
- [x] マニュアルの削除機能
- [x] サンプル動画をGCS直通にする
- [x] ハイライトの線が一定じゃない
